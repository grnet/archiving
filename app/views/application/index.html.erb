<%= link_to 'admin', login_path(admin: 'admin'), method: :post, role: :button,
  class: 'btn btn-primary' %>

<%= link_to 'vima', vima_path(vima: 'vima'), method: :post, role: :button,
  class: 'btn btn-primary' %>
